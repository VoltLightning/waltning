diff --git a/web/WorkerChannel.ts b/web/WorkerChannel.ts
index 7fba380845e3338b7d8d253013fbf73896070cde..d7a5715f199898f0c375597b720eae5589065b34 100644
--- a/web/WorkerChannel.ts
+++ b/web/WorkerChannel.ts
@@ -40,7 +40,9 @@ export function sendWorkerResult({
     const resultJson = error != null ? serialize({ error }) : serialize({ result });
     const resultBytes = new TextEncoder().encode(resultJson);
     const length = resultBytes.length;
-    resultArray.set(new Uint32Array([length]), 0);
+    // A typed-array `set` copies elements, not bytes: the length arrived as
+    // `length & 0xFF` and every sync result over 255 bytes was truncated.
+    new DataView(resultBuffer).setUint32(0, length, true);
     resultArray.set(resultBytes, 4);
     Atomics.store(lock, 0, RESOLVED);
   } else {
