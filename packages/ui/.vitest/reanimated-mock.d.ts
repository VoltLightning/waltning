/**
 * Reanimated publishes types for its root only; the built mock has none.
 * Declared as a module so `reanimated.ts` can import and cast it once.
 */
declare module "react-native-reanimated/lib/module/mock";
